# Скопируй этот файл в .env и впиши свои значения.
# .env в git НЕ попадает (см. .gitignore). Сюда токен НЕ пишем.

# Токен бота от BotFather (используй НОВЫЙ, перевыпущенный через /revoke)
BOT_TOKEN=

# Ключ LLM-API — понадобится позже, на этапе анализа. Пока можно оставить пустым.
LLM_API_KEY=
